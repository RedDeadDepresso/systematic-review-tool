# Application configuration
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
SECRET_KEY='django-insecure-_-os##p$byrf^i1srx!_#zf0l(pg7tu!q0xsbw8&is%9pa@e9('
# Encryption key to store zotero api keys generated in python using:
# from cryptography.fernet import Fernet
# print(Fernet.generate_key().decode())
ENCRYPTION_KEY=
SITE_DOMAIN=localhost:8000
FRONTEND_BASE_URL=localhost:3000 # Frontend url used in password reset emails sent
PASSWORD_RESET_CONFIRM_PATH=/confirm-password-reset


# Email configuration (If Using SMTP i.e. django.core.mail.backends.smtp.EmailBackend)
EMAIL_HOST=smtp.gmail.com  # host for sending emails, gmail example
EMAIL_HOST_USER=your-email@example.com # The email address that will be used to send emails.
EMAIL_HOST_PASSWORD=your-password # App password, you can get one from https://support.google.com/accounts/answer/185833?hl=en if using gmail


# Backend api url used by frontend. Should be same as http:// + SITE_DOMAIN + /api
# After setting ssl certificate change http to https
VITE_API_BASE_URL=http://localhost:8000/api


# Backend websocket url used by frontend. Should be same as ws:// + SITE_DOMAIN + /ws
# After setting ssl certificate change ws to wss
VITE_WS_URL=ws://localhost:8000/ws